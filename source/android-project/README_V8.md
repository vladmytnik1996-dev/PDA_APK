# КПК Зоны 1.0.6 — Release 8 GPS Calibration QR

- release package: `by.smorhon.stalkerpda.v3`;
- debug package: `by.smorhon.stalkerpda.v8.debug`;
- `versionName`: `1.0.6`;
- `versionCode`: `10006`;
- `minSdk`: 24;
- `targetSdk`: 35.

Главное изменение веб-части — передача через QR только GPS-привязки карты. Android-оболочка остаётся полноэкранной и совместимой с установленной версией 1.0.5.

Для сборки debug: `./build.sh debug`. Для release нужны приватные материалы подписи, которые намеренно не включены в публичный архив.
